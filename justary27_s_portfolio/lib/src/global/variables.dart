import 'package:flutter/material.dart';

GlobalKey<NavigatorState> navbarRouter = GlobalKey<NavigatorState>(
  debugLabel: "Navbar Router Used",
);

GlobalKey<NavigatorState> siteRouter = GlobalKey<NavigatorState>(
  debugLabel: "Site Router Used",
);
